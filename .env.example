# Node Environment (development, production, etc.)
NODE_ENV=development

# Server Configuration
SERVER_PORT=2634

# Google Cloud Platform Configuration
GCP_PROJECT_ID="your-gcp-project-id" 
GCP_REGION="us-central1" # Or your preferred region
GCS_BUCKET_NAME="your-unique-gcs-bucket-name" 

# Path to your Google Cloud Service Account Key JSON file
# Example: /Users/your_user/keys/gcp-service-account.json
# Make sure this file is NOT committed to version control.
GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/service-account-key.json"

# Optional: SSL Configuration (if you are running HTTPS locally)
# SSL_KEY_PATH=path/to/ssl/key
# SSL_CERT_PATH=path/to/ssl/cert 